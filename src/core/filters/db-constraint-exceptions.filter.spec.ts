import { DbConstraintExceptionsFilter } from './db-constraint-exceptions.filter';

describe('DbConstraintExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new DbConstraintExceptionsFilter()).toBeDefined();
  });
});
